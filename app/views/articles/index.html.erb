<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title></title>
</head>

<div class="container">
  <div style="border-bottom:1px solid #000; vertical-align: middle">
    <div align="left"><h1 style="margin-top:10px; text-align:center;">Welcome to Blog Application</h1></div>
    <div align="right">
      <% if user_signed_in? %>
          Logged in as <strong><%= current_user.email %></strong>
      <% end %>
    </div>
  </div>
  <span style="margin-left:2px; margin-top:5px;"></span>

  <%= render @articles %>
  <br>
  <%= button_to 'Create New Article', new_article_path, :class => 'btn btn-large btn-primary', :method => :get %>
</div>

</html>